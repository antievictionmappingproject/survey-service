{
	"_links": {
        "self": { "href": "<%= @base_url %>/surveys" },
        "items": [
        	<% @surveys.each do |survey| %>
        	{ "href": "<%= @base_url %>/surveys/<%= survey.id%>" }
  		    <% end %>
  		]
    }
}